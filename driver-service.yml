server:
  port: 8081

spring:
  application:
    name: driver-service

  datasource:
    url: jdbc:mysql://localhost:3306/driver_db
    username: root
    password: Ritika@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

  instance:
    hostname: localhost
    prefer-ip-address: true
